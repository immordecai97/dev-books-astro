---
	import { SCORE_API_ENDPOINT } from "astro:env/client";
	const { id } = Astro.props;
	const res = await fetch(SCORE_API_ENDPOINT);
	const scoreValue = await res.text();
---

<span id="score" class="text-xs">
	⭐ Puntuación {scoreValue} de 5
</span>